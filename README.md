# Overview

Just a simple example of the power of the Google Closure Compiler[1].

# Installing closure compiler
```
  brew install closure-compiler
```

# Running the test:

```
closure-compiler --compilation_level ADVANCED_OPTIMIZATIONS --js hello.js > hello.final.js
```

1 - https://developers.google.com/closure/compiler/
